1) Запустить скрипт 1.sh

Файл part_types.txt нужен для того, чтобы по шестнадцатеричному id определять словестное описание типа раздела.

Для каждой непустой записи в таблице разделов (считаем, что пустая запись не задаёт раздел) выводится его индекс в таблице и тип. В конце выводится число всех заданных разделов. 
